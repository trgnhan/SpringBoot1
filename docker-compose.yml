version: '3.9'

services:
  api-service:
    container_name: api-container1
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - '8080:80'
    env_file:
      - .env
    networks:
      - backend

networks:
  backend:
    driver: bridge