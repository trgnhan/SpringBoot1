spring:
  config:
    activate:
      on-profile: dev
server:
  port: 8080
open:
  api:
    title: API document
    version: v1.0.0
    description: Mota API service
    serverUrl: http://localhost:${server.port}
    serverName: server dev

